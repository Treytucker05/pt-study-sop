# Core runtime (dashboard)
Flask>=3.0,<4
requests>=2.31,<3

# Optional: calendar assistant (OpenAI / OpenRouter via OpenAI SDK-compatible client)
openai>=1.0,<2

# Optional: Google Calendar integration
google-auth>=2.0,<3
google-auth-oauthlib>=1.0,<2
google-api-python-client>=2.0,<3

# Optional: Blackboard scraper (scripts/scrape_blackboard.py)
python-dotenv>=1.0,<2
selenium>=4.0,<5
webdriver-manager>=4.0,<5

# Dev/test
pytest>=8.0,<10
