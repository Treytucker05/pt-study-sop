id: M-ENC-009
name: Concept Map
category: encode
control_stage: ENCODE
default_duration_min: 10
energy_cost: high
has_steps: Y
has_outputs_or_artifacts: Y
outputs_summary: "Concept map (nodes + labeled links); Mermaid code"
knobs:
  diagram_format:
    type: enum
    options: [mermaid, ascii, markdown_list]
    default: mermaid

facilitation_prompt: |
  Analyze the skeleton outline just provided and generate a concept map visualizing the relationships between the core components. You MUST format your visual output strictly using {diagram_format}.
