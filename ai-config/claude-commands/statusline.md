---
description: "Configure the Claude Code statusline to show context usage and git branch."
---

# Statusline (Claude Code)

Goal: always show context usage and current git branch.

Steps:
1) Run `/statusline`.
2) Enable fields:
   - Context usage (%)
   - Git branch
   - CWD (optional)
3) Save as default.
