# Questions Needed - 2026-01-11
- Should audits use a fallback sample when the 7-day window is empty, or remain strict with empty-window reporting?
- Should a pre-probe be required even when the learner reports zero prior exposure?
- Should ingest reject template-only logs automatically?
