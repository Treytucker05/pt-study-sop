# Specification: Agent Canon Follow-Up

**Track ID:** agent-canon-followup_20260306
**Type:** Infra
**Created:** 2026-03-06
**Status:** Draft

## Summary

Perform a follow-up cleanup on the new agent canon setup by reducing repeated inheritance boilerplate in the global Claude agent catalog, improving `scripts/sync_agent_config.ps1` output so validation summaries are visible in shell hooks and direct runs, and documenting the optimized setup clearly.

## Context

The previous track aligned repo and global agent surfaces around root `AGENTS.md` and repaired Codex subagent startup. Two follow-up gaps remain:

1. Global Claude agents repeat the same long inheritance block in every file.
2. `scripts/sync_agent_config.ps1` exits correctly, but its output is too sparse in direct shell runs and hooks.
3. The final setup should be explained back to the user as an operator walkthrough.

## Acceptance Criteria

- Global Claude agents keep the same inheritance behavior with materially less duplicated boilerplate.
- `scripts/sync_agent_config.ps1` prints a clear full summary and final result line in all modes.
- Validation still passes after the cleanup.
- Track docs/logs reflect the follow-up change set.

## Non-goals

- Another broad canon rewrite.
- Renaming global or repo-local agents.
- Changing Codex runtime behavior beyond what is already fixed.

---

_Generated by Conductor Track Plan._
