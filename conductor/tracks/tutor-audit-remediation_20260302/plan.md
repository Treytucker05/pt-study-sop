# Implementation Plan: Tutor Audit Remediation (P0/P1)

**Track ID:** tutor-audit-remediation_20260302
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-02
**Status:** [x] Complete

## Overview

Four targeted fixes to align the Tutor API and frontend contracts. Each fix is independent and testable in isolation. Phase 1 covers the backend fixes (Python), Phase 2 covers the frontend fixes (TypeScript), Phase 3 runs full verification.

## Phase 1: Backend Fixes (api_tutor.py)

Backend contract alignment and artifact type mapping.

### Tasks

- [x] Task 1.1: Add `duration_seconds`, `artifact_count`, `objectives_covered` to session summary response
- [x] Task 1.2: Return `chain_progress` as structured object `{ current_block, total_blocks, chain_name }`
- [x] Task 1.3: Map `table` → `note` and `structured_map` → `map` in `create_artifact`

### Verification

- [x] `pytest brain/tests/ -k tutor` passes with new regression tests

## Phase 2: Frontend Fixes (api.ts, TutorArtifacts.tsx)

Fix save-wrap URL construction and consume new backend fields.

### Tasks

- [x] Task 2.1: Refactor `getSessionSummary` to accept `opts?: { save?: boolean }` and build URL with query param
- [x] Task 2.2: Update `TutorArtifacts.tsx` save-wrap call to use new signature
- [x] Task 2.3: Frontend type interfaces already match (TutorSessionWrapSummary was correct)

### Verification

- [x] Frontend build succeeds (`npm run build` in dashboard_rebuild/)
- [x] Manual smoke test: save-wrap URL no longer 404s
  - verified live on `2026-03-13` via `GET /api/tutor/session/<id>/summary?save=true` -> `200` with `wrap_saved.saved=true`

## Phase 3: Regression Tests + Final Verification

### Tasks

- [x] Task 3.1: Add `test_get_session_summary_returns_duration_seconds_and_artifact_count`
- [x] Task 3.2: Add `test_create_artifact_table_stored_as_note`
- [x] Task 3.3: Add `test_create_artifact_structured_map_stored_as_map`
- [x] Task 3.4: Run full test suite — 10/10 passing

### Verification

- [x] All acceptance criteria from spec.md met
- [x] New regression tests green (10/10)
- [x] Frontend build clean
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
