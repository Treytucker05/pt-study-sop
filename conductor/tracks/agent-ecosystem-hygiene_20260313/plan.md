# Implementation Plan: Agent Ecosystem Hygiene

**Track ID:** agent-ecosystem-hygiene_20260313
**Spec:** [./spec.md](./spec.md)
**Created:** 2026-03-13
**Status:** [x] Complete

## Phase 0: Track Setup

- [x] Task 0.1: Claim scope in `docs/root/TUTOR_TODO.md`, `docs/root/AGENT_BOARD.md`, `conductor/tracks.md`, `conductor/tracks/GENERAL/log.md`, and create the baseline track files.

## Phase 1: Evidence And Topology Freeze

- [x] Task 1.1: Capture runtime evidence for Claude, Codex, Cursor, OpenCode, Antigravity, Kimi, and Conduit.
- [x] Task 1.2: Capture Gemini-specific evidence for `C:\Users\treyt\.gemini\skills`, embedded Antigravity roots, and `C:\Users\treyt\.gemini\GEMINI.md`.
- [x] Task 1.3: Freeze the supported topology, legacy roots, local exceptions, and cleanup policy from the evidence.

## Phase 2: Safety Rails And Sync Hardening

- [x] Task 2.1: Create rollback artifacts for every mutable external root before any repair.
- [x] Task 2.2: Repair and fixture-test `scripts/sync_agent_skills.ps1` against the frozen topology.
- [x] Task 2.3: Decide whether the embedded Gemini Antigravity subtree is supported or quarantined.

## Phase 3: Skill Root Repair

- [x] Task 3.1: Apply the repaired sync to supported active non-embedded roots and prove idempotence.
- [x] Task 3.2: Execute the chosen Gemini embedded-root branch and verify the result.

## Phase 4: Config Secret Hardening

- [x] Task 4.1: Audit supported and legacy config secret exposure and choose non-plaintext injection paths.
- [x] Task 4.2: Patch supported active config to the approved non-plaintext injection path.
- [x] Task 4.3: Rotate exposed credentials and record smoke evidence or an explicit blocker.

## Phase 5: Docs And Close-Out

- [x] Task 5.1: Update repo/operator docs and validation guidance to the frozen topology.
- [x] Task 5.2: Update external fallback docs only where runtime proof says they are consumed.
- [x] Task 5.3: Run final validation, update board/log surfaces, and close out the track.

## Verification Gates

- `scripts/sync_agent_skills.ps1 -Mode DryRun`
- `scripts/sync_agent_skills.ps1 -Mode Apply`
- `scripts/sync_agent_skills.ps1 -Mode Check`
- supported-root broken-junction scans
- redacted secret scan over supported active config
- `scripts/sync_agent_config.ps1 -Mode Check`
- `git diff --check`

## Working Notes

- Do not delete canonical shared skills from `C:\Users\treyt\.agents\skills` as part of this track.
- Preserve proven tool-local exceptions unless runtime evidence shows they are obsolete.
- Treat Kimi and Conduit as config-only until runtime proof shows a supported skill surface.
- Do not treat `C:\Users\treyt\.gemini\antigravity\mcp_config.json` secret removal as complete until the post-patch redacted scan passes.

## Verification Summary

- Rollback and fixture proof:
  - `powershell -ExecutionPolicy Bypass -File scripts/test_sync_agent_skills_fixture.ps1` -> PASS
  - `C:\Users\treyt\.agents\backups\agent-ecosystem-hygiene_20260313\manifest.json` created before home-directory mutation
- Supported-root repair proof:
  - `powershell -ExecutionPolicy Bypass -File scripts/sync_agent_skills.ps1 -Mode DryRun` -> PASS
  - `powershell -ExecutionPolicy Bypass -File scripts/sync_agent_skills.ps1 -Mode Apply` -> PASS
  - `powershell -ExecutionPolicy Bypass -File scripts/sync_agent_skills.ps1 -Mode Check` -> PASS
  - second `powershell -ExecutionPolicy Bypass -File scripts/sync_agent_skills.ps1 -Mode Check` -> PASS
  - supported-root broken-junction scan -> Claude/Codex/Cursor/OpenCode/Gemini/Antigravity all `81` junctions, `0` broken
- Config hardening proof:
  - redacted config scan over in-scope files -> `0` findings after patch
  - GitHub MCP startup smoke with env-backed token -> PASS
  - embedded Obsidian MCP startup smoke -> FAIL with `Usage: mcp-obsidian <vault-directory>` on the quarantined legacy config
- Repo/doc validation:
  - `python scripts/check_docs_sync.py` -> PASS
  - `powershell -ExecutionPolicy Bypass -File scripts/sync_agent_config.ps1 -Mode Check` -> PASS
  - `git diff --check` -> PASS (CRLF warnings only)

## Close-Out Notes

- Supported active roots now mirror the valid shared package set and no longer carry broken or stale projection names.
- `C:\Users\treyt\.gemini\GEMINI.md` is now a thin precedence-only fallback because installed Gemini extensions actively consume it.
- `C:\Users\treyt\.gemini\antigravity\mcp_config.json` no longer stores plaintext secrets inline, but the exposed credentials still require operator rotation.
- The canonical upstream still contains two malformed non-package directories (`artifacts-builder`, `ui-ux-pro-max`); they were intentionally left upstream and excluded from projection because this track repaired the mirror contract rather than pruning shared upstream directories.

---

_Generated by Conductor Plan._
