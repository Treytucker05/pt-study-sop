Run from repo root `C:\pt-study-sop`.

Why these commands: they are the canonical guardrail checks listed in `docs/project/PARALLEL_AGENT_WORKFLOW.md:102` and mirrored in `scripts/install_agent_guard_hooks.ps1:117`.

1. Docs sync check  
`python scripts/check_docs_sync.py`  
Pass criteria: prints `Docs sync check passed.` and exits `0`. Fails (`exit 1`) if README/runtime version/required guide phrases/CLAUDE pointer/validator presence checks fail (`scripts/check_docs_sync.py:15`).

2. Project hub check  
`python scripts/validate_project_hub.py`  
Pass criteria: prints `Project Hub validation passed.` and exits `0`. Fails (`exit 1`) on missing required `docs/project/*.md` files or broken relative links in `docs/project/INDEX.md` (`scripts/validate_project_hub.py:11`).

3. `sync_agent_config` check  
`pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\sync_agent_config.ps1 -Mode Check`  
Pass criteria: exits `0` only if required root files exist, `.claude` stubs match expected content, and config JSON files are valid. In `Check` mode any `MISSING`/`MISMATCH`/`INVALID` causes `exit 1` (`scripts/sync_agent_config.ps1:45`, `scripts/sync_agent_config.ps1:122`).

Launch-wizard context (read-only inspection):
- Exists: `scripts/parallel_launch_wizard.ps1`
- Exists: `scripts/opencode_wsl_tmux.sh`
- Exists: `docs/project/PARALLEL_AGENT_WORKFLOW.md`
- Exists: `scripts/README.md`
- Exists: `C:\Users\treyt\OneDrive\Desktop\Travel Laptop\Parallel Work`  
- Launcher files found there: `00_Setup_Worktrees_and_Hooks.bat`, `01_Launch_Parallel_Wizard.bat`, `10_Launch_Codex_Parallel.bat`, `11_Launch_Claude_Parallel.bat`, `12_Launch_OpenCode_Parallel.bat`, `20_Launch_Swarm_All_Agents.bat`, `90_Status_Check.bat`.

No missing path/launcher detected.

```text
[PARALLEL_AGENT_WORKFLOW.md]
        |
        +--> check commands (sync_agent_config / docs_sync / project_hub)
        |
        +--> parallel_launch_wizard.ps1
                 |
                 +--> opencode_wsl_tmux.sh (optional WSL tmux)
                 +--> Desktop "Parallel Work" .bat launchers
```