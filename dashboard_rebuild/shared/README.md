# Shared Types

Exports Drizzle schema types for client/server use.
System context: shared types support the CP-MSS v1.0 pipeline used by Tutor and session flows.

## Files
- `schema.ts` - re-exports from `../schema.ts`.
