# requirements-tools.txt — SOP library tooling (never imported at runtime)
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-tools.txt
PyYAML>=6.0,<7
pydantic>=2.0,<3
